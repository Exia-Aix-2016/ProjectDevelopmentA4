<Window x:Class="Client.Processing"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Processsing" Height="450" Width="800"
        AllowsTransparency="True" Background="Transparent"
        WindowStyle="None" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>

    </Window.Resources>
    <Grid>
        <Grid>
            <Border CornerRadius="5" Grid.RowSpan="2">
                <Border.Background>
                    <LinearGradientBrush>
                        <GradientStop Color="ForestGreen" Offset="0.0"/>
                        <GradientStop Color="MediumSeaGreen" Offset="1"/>
                    </LinearGradientBrush>

                </Border.Background>
                <TextBlock TextWrapping="Wrap" Text="Vos documents sont en traitement" Margin="111,111,111,111"
                    FontFamily="Helvetica" 
                    FontSize="40"
                    Foreground="BlueViolet"
                    TextAlignment="Center"/>
                     
            </Border>
           
        </Grid>
        <Grid Margin="20">
            <ProgressBar Minimum="0" Maximum="100" IsIndeterminate="True" Margin="147,287,147,86" />
        </Grid>
    </Grid>
    
    
    
</Window>
